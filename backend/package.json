{
  "name": "proiect3-backend",
  "version": "1.0.0",
  "description": "Backend for Project 3 - Employee Filter",
  "main": "server.js",
  "scripts": {
    "start": "concurrently \"json-server --watch db.json --port 3000\" \"node server.js\"",
    "dev": "concurrently \"json-server --watch db.json --port 3000\" \"nodemon server.js\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "joi": "^17.11.0",
    "json-server": "^1.0.0-beta.15"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
